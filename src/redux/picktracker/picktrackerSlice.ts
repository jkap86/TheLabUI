import { createSlice } from "@reduxjs/toolkit";

export interface PicktrackerState {}

const initialState: PicktrackerState = {};

const picktrackerSlice = createSlice({
  name: "picktracker",
  initialState,
  reducers: {},
});
